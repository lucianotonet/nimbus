(function ($, root, undefined) {
	
	$(function () {
		
		'use strict';

		// $('#page_template option').each(function(index, el) {
		// 	var template = $(this).attr('value');
		// 	template = template.replace('.php', '');
		// 	template = template.replace('template-', '');
		// 	console.log(template);
		// 	$( '#'+template ).css('display', 'none');
		// });
		
		// $('#page_template').change(function(event) {
		// 	var template = $(this).val();
		// 	template = template.replace('.php', '');
		// 	template = template.replace('template-', '');
		// 	$( '#'+template ).css('display', 'block');
		// });
		
	});
	
})(jQuery, this);
